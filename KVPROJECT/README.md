# Project (Password Protected)

## Password
`HEMANSHU`

## How to run
- Open `index.html` in a browser (double-click), or
- Use any static server (recommended) and open the shown local URL.

Example (PowerShell):
```powershell
cd "C:\Users\hp\Desktop\KVPROJECT"
python -m http.server 5500
```
Then open `http://localhost:5500`.

